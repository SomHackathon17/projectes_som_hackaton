using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Gosht_Script : MonoBehaviour {

	//public static PacmanPowerUp = false;
	/*

	 
	public float speed = 4.0f;
	public Transform target;
	public Color color = Color.white;

	private List<Node> path;
	private Pathfinding pf;

	// Use this for initialization
	void Start () {
		color.a = 0.5f;
		pf = GetComponent<Pathfinding> ();
	}
	
	// Update is called once per frame
	void Update () {
		path = pf.FindPath (this.transform.position, target.position);
		this.transform.position = Vector3.MoveTowards (this.transform.position, path [1].WorldPosition, speed * Time.deltaTime);
	}

	void OnDrawGizmos()
	{
		if (pf != null) {
			foreach (Node n in pf.g.grid_1)
			{
				if(path != null && path.Contains(n))
				{
					Gizmos.color = color;
					Gizmos.DrawCube (n.WorldPosition, Vector3.one * (float)pf.g.node_size * 0.9f);
					
					}
			}
		}

	}


	*/


}
